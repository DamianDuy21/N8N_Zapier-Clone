1. Bình thường dự án mình hay dùng mongodb là database, ở đây dùng postgres thông qua giao diện Noen, nó giống như dùng mongodb bản web ấy
-> postgres thay cho mongodb

2. Backend mình hay dùng express thì ở đây dùng prisma để thao tác với database
3. Về giao thức truyền thông giữa Be và Fe, bình thường mình dùng Restful API, bây giờ dùng tRPC. Đây là 1 data access layer giúp bảo mật, an toàn hơn. 
-> trpc sẽ có .query, .mutation, ... để thay cho post, get, put, ...

4. Authentication dùng BetterAuth
5. Theme cho shadcn dùng tweakcn, Logo lấy ở https://logoipsum.com/
6. Background jobs dùng https://www.inngest.com/?utm_source=youtube&utm_medium=video&utm_campaign=cwa-3&utm_term=next.js
7. Error tracking dùng Sentry, có logs và session replays, cái session replays cho phép xem được là lỗi này do người dùng bấm vào UI nào, nghĩa là không chỉ logs ở Be mà còn xem được nguyên nhân từ Fe
8. Sidebar Layout
9. Payments Setup dùng Polar